dw:
    worker_node:
        ros__parameters:
            use_gpu: True
            number:
                states: 12
                actions: 6
                iterations: 300 #4000
                episodes: 10000
            hyperparameter:
                alpha: 0.007
                gamma: 0.99
                epsilon: 0.5
            database:
                local_size: 10000
            decay_rate: 1
            hidden_layers: [64,64]
